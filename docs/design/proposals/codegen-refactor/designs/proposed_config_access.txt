title Proposed Config/API Inference Access

participant SetResource
participant Model
participant ackgenconfig.CodeGenerate
participant ackgenconfig.Inference

SetResource->Model: m.GetShape(r, fieldName)
Model<->ackgenconfig.Inference: <calls inference helpers to resolve `aws-sdk` and `ackgenconfig`>
Model->SetResource: return shape, nil
SetResource->Model: m.GetFieldPath(r, fieldName)
Model<->ackgenconfig.CodeGenerate: <calls generate helpers to fetch Go code generating configs>
Model->SetResource: return fieldPath